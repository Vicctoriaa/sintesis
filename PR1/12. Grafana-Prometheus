# CT 101 — Grafana + Prometheus

## Datos del contenedor

| Campo | Valor |
|-------|-------|
| CT ID | 101 |
| Hostname | Grafana-Prometheus |
| OS | Debian 12 |
| Memoria | 512 MB |
| Disco | 6 GB (local-lvm) |
| Cores | 1 |
| Privilegiado | Sí |
| Features | lxc.apparmor.profile: unconfined |

## Red

| Campo | Valor |
|-------|-------|
| Bridge | vmbr1 |
| VLAN | 30 — SOC |
| IP | 10.1.1.66/27 |
| Gateway | 10.1.1.65 |

---

## Prometheus

### Instalación

```bash
wget https://github.com/prometheus/prometheus/releases/download/v2.45.0/prometheus-2.45.0.linux-amd64.tar.gz -O /tmp/prometheus.tar.gz
tar xvf /tmp/prometheus.tar.gz -C /opt/
mv /opt/prometheus-2.45.0.linux-amd64 /opt/prometheus
```

### Servicio systemd

```bash
cat > /etc/systemd/system/prometheus.service << 'EOF'
[Unit]
Description=Prometheus
After=network.target

[Service]
Type=simple
ExecStart=/opt/prometheus/prometheus \
  --config.file=/opt/prometheus/prometheus.yml \
  --storage.tsdb.path=/opt/prometheus/data \
  --web.listen-address=0.0.0.0:9090
Restart=always

[Install]
WantedBy=multi-user.target
EOF

systemctl daemon-reload
systemctl enable prometheus
systemctl start prometheus
```

### Configuración `/opt/prometheus/prometheus.yml`

```yaml
global:
  scrape_interval: 15s
  evaluation_interval: 15s

alerting:
  alertmanagers:
    - static_configs:
        - targets:

rule_files:

scrape_configs:
  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]

  - job_name: "node_exporter"
    static_configs:
      - targets:
        - "10.1.1.66:9100"    # CT101 Grafana-Prometheus
        - "10.1.1.34:9100"    # CT103 DNS
```

### Targets activos

| Target | IP | Estado |
|--------|----|--------|
| Prometheus | localhost:9090 | up ✅ |
| Node Exporter CT101 | 10.1.1.66:9100 | up ✅ |
| Node Exporter CT103 | 10.1.1.34:9100 | up ✅ |

---

## Node Exporter

### Instalación

```bash
wget https://github.com/prometheus/node_exporter/releases/download/v1.7.0/node_exporter-1.7.0.linux-amd64.tar.gz -O /tmp/node_exporter.tar.gz
tar xvf /tmp/node_exporter.tar.gz -C /opt/
mv /opt/node_exporter-1.7.0.linux-amd64 /opt/node_exporter
```

### Servicio systemd

```bash
cat > /etc/systemd/system/node_exporter.service << 'EOF'
[Unit]
Description=Node Exporter
After=network.target

[Service]
Type=simple
ExecStart=/opt/node_exporter/node_exporter
Restart=always

[Install]
WantedBy=multi-user.target
EOF

systemctl daemon-reload
systemctl enable node_exporter
systemctl start node_exporter
```

> Node Exporter debe instalarse en cada contenedor que se quiera monitorizar. Escucha en el puerto `9100`.

---

## Grafana

### Instalación via Docker Compose

Fichero `~/grafana/docker-compose.yml`:

```yaml
services:
  grafana:
    image: grafana/grafana-oss:latest
    container_name: grafana
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - grafana-storage:/var/lib/grafana
volumes:
  grafana-storage:
```

### Arranque

```bash
cd ~/grafana && docker compose up -d
```

### Acceso

| Campo | Valor |
|-------|-------|
| URL interna | http://10.1.1.66:3000 |
| URL externa | http://192.168.3.200 (via Nginx CT105) |
| Usuario | admin |
| Password | admin (cambiar en primer acceso) |

---

## Comandos útiles

```bash
# Ver estado de servicios
systemctl status prometheus
systemctl status node_exporter
docker ps

# Reiniciar servicios
systemctl restart prometheus
systemctl restart node_exporter
docker compose -f ~/grafana/docker-compose.yml restart

# Ver logs Grafana
docker logs grafana

# Ver targets Prometheus
curl -s http://localhost:9090/api/v1/targets | grep health
```

---

## Resumen

```
CT 101 — Grafana-Prometheus (VLAN 30 · 10.1.1.66)
   ├── Prometheus    → :9090  (recopila métricas)
   ├── Node Exporter → :9100  (métricas del sistema)
   └── Grafana       → :3000  (visualización, via Docker)
```
